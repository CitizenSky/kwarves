{
  "name": "kwarves-dashboard",
  "version": "2026.06.02",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "dev:restore": "cp index.src.html index.html && rm -rf assets/ schema/",
    "build": "vite build --base=/kwarves/dashboard/ && cp dashboard-data.js dist/ && cp dashboard-notifications.js dist/",
    "deploy": "cp index.html index.src.html && cp -r dist/* . && rm -rf dist/",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}